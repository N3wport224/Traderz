# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
*.db
*.db-journal
*.db-shm
*.db-wal

# PyInstaller build output (see build_executable.bat / trading_platform.spec)
/build/
/dist/

# Node / Next.js
node_modules/
frontend/.next/
frontend/out/
frontend/.env*.local

# Secrets — real credentials never enter git (see CLAUDE.md rule 6).
# `.env.example` is the only tracked env file; it must contain placeholders only.
.env
.env.*
!.env.example

# Editors / OS
.DS_Store
.vscode/
.idea/
logging.json
