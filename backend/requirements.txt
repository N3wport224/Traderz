fastapi>=0.115
uvicorn[standard]>=0.30
pandas>=2.2
numpy>=1.26
websockets>=12.0
httpx>=0.27
pytest>=8.0
pytest-asyncio>=0.23
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.20
greenlet>=3.0

# Required for LIVE CRYPTO market data (DATA_SOURCE_MODE=live with a crypto
# pair polls public CCXT OHLCV) and for live crypto execution
# (GATEWAY_MODE=live). Imported lazily, so pure-mock installs still run
# without it — but without it the live BTC/USDT feed reports
# DATA_DISCONNECTED, so it ships installed by default.
ccxt>=4.3
